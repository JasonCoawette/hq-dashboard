export default function H2({ label }: { label: string }) {
  return <h2 className="font-sans text-[22px] subpixel-antialiased font-semibold tracking-[-0.02em] leading-none text-primaryFG">{label}</h2>;
}
