export default function H2({ label }: { label: string }) {
  return <h2 className="text-[16px] lg:text-[20px] xl:text-[24px] font-geist font-semibold leading-none tracking-[-0.04em] text-primaryFG">{label}</h2>;
}
